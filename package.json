{
  "name": "nuxt-sample-module",
  "version": "24.0.0-alpha.3",
  "description": "Testing code",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/batopa/nuxt-sample-module.git"
  },
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/types.d.mts",
      "import": "./dist/module.mjs"
    }
  },
  "main": "./dist/module.mjs",
  "typesVersions": {
    "*": {
      ".": [
        "./dist/types.d.mts"
      ]
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepack": "nuxt-module-build build",
    "dev": "nuxi dev playground",
    "dev:build": "nuxi build playground",
    "dev:prepare": "nuxt-module-build build --stub && nuxt-module-build prepare && nuxi prepare playground",
    "release": "npm run prepack && changelogen --release && git push --follow-tags && npm publish --access public",
    "release:patch": "npm run prepack && changelogen --release --patch --push && npm publish --access public",
    "release:minor": "npm run prepack && changelogen --release --minor --push && npm publish --access public",
    "release:major": "npm run prepack && changelogen --release --major --push && npm publish --access public",
    "release:alpha": "npm run prepack && changelogen --release --premajor alpha --push && npm publish --access public",
    "release:beta": "npm run prepack && changelogen --release --premajor beta --push && npm publish --access public",
    "release:rc": "npm run prepack && changelogen --release --premajor rc --push && npm publish --access public",
    "pre-release:alpha": "npm run prepack && changelogen --release --prerelease alpha --push && npm publish --access public",
    "pre-release:beta": "npm run prepack && changelogen --release --prerelease beta --push && npm publish --access public",
    "pre-release:rc": "npm run prepack && changelogen --release --prerelease rc --push && npm publish --access public",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@nuxt/kit": "^4.0.3"
  },
  "devDependencies": {
    "@nuxt/devtools": "^2.6.2",
    "@nuxt/eslint-config": "^1.8.0",
    "@nuxt/module-builder": "^1.0.2",
    "@nuxt/schema": "^4.0.3",
    "@nuxt/test-utils": "^3.19.2",
    "@types/node": "latest",
    "changelogen": "^0.6.2",
    "eslint": "^9.32.0",
    "nuxt": "^4.0.3",
    "typescript": "~5.8.3",
    "vitest": "^3.2.4",
    "vue-tsc": "^3.0.5"
  }
}
